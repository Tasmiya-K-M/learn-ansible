---
- name: Simple Collections Demo
  hosts: localhost
  gather_facts: no
  collections:
    - community.general   # Declaring collection (conceptual)

  tasks:
    - name: Say hello (built-in module)
      command: echo "Hello from collections demo"
      register: result

    - name: Show the output
      debug:
        var: result.stdout