---
- name: Async demo
  hosts: worker
  tasks:
    - name: service restart
      service:
        name: sample service
        state: restarted
      ignore_errors: yes
    - name: print statemnet
      debug:
        msg: hello from playbook
      register: result
      failed_when: result.rc != 0